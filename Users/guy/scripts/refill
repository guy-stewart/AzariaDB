

// fill up on nystrom
warp(S17_bMINE);
drop(SMP_RECYCLE);
grab(SMP_VIAL);
if (got() == IDD_SCOOPF) { drag(SMP_VIAL);}

write("got ", got());
while (got() == IDD_SCOOPE) {
    drag(S17_bMINE);
    drag(SMP_VIAL);
    write("got ", got());
}
